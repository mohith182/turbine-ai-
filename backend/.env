# Email configuration - OTP will be printed to console if not configured
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# JWT Secret
JWT_SECRET=turbineai-dev-secret-key-change-in-production

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000
